# UI / 設計規範（Design System）— <PROJECT NAME>

<!--
本文件是視覺規範的權威來源。CONSTITUTION.md 的「視覺設計」段落只放一句
指標指向這裡，不重複內容——細節規格改這裡，CONSTITUTION.md 不用跟著動。
architect / developer / reviewer 在涉及畫面的 Story 中應讀取本文件。

這是骨架範本，不是可直接套用的規範——章節結構沿用一次實際 pilot 驗證過
的分類方式，但內容全部要換成你的專案自己的視覺語言。沒有視覺畫面需求的
專案（純 API 服務等）可以直接刪除這份檔案，並把 CONSTITUTION.md 的
「視覺設計」段落一併移除。
-->

## 0. 風格定位

TODO：一到兩句話描述整體視覺方向（例如參考基準、關鍵字：極簡/編輯感/
活潑/企業感等）。若有具體參考網站/設計稿，列出來源並註明依專案語言/
內容特性做了哪些調整（例如中文排版對西式參考的取捨）。

## 1. 色彩系統

TODO：用 CSS 自訂屬性列出色彩 token（背景、文字主色/次色、邊框、強調色
——是否需要強調色是本節第一個要決定的問題），並註明對比度要求
（建議至少 WCAG AA 4.5:1）。

| Token | 值 | 用途 |
|---|---|---|
| `--color-bg` | | |
| `--color-text-primary` | | |
| `--color-text-secondary` | | |
| `--color-border` | | |
| `--color-accent` | | |

## 2. 字體

TODO：標題/內文字體家族（是否貫穿全站同一套、或標題內文分開），是否
額外載入 web font（要考量載入效能與是否有建置流程）。

## 3. 字級與字重階層

TODO：至少涵蓋主標題/次標題/內文/輔助文字（meta）幾個層級的
font-size/line-height/font-weight。

| 層級 | 用途 | font-size | line-height | font-weight |
|---|---|---|---|---|
| | | | | |

## 4. 間距 Scale

TODO：固定倍數的間距 token（例如 4px 基準），所有 padding/margin/gap
只能取用這組值，不寫魔術數字。

## 5. RWD 斷點

TODO：手機/平板/桌機斷點值。**若程式碼已有既有頁面，先去查現有斷點值
沿用，不要另外發明一套**（比照 `docs/04` 既有專案套用補充的精神）。

## 6. 對齊規則

TODO：標題/meta/內文各自的對齊方式。

## 7. 元件規則

TODO：連結、按鈕（變體）、表單欄位、卡片/列表項、導覽列等常見元件的
樣式規則。

## 8. 無障礙基本要求

TODO：至少涵蓋色彩對比、表單 label、可見的 focus 樣式、必填欄位不能
只靠顏色標示這幾項——這幾項本身可機器檢查，適合直接當驗收條件。

## 9. 技術落地方式

TODO：色彩/字級/間距等 token 要用什麼實際機制在專案裡落地（例如一份
共用的 CSS 自訂屬性檔案），前後端/多個輸出目標各自怎麼消費這份檔案。

## 10. 套用範圍策略

TODO：新規範是否強制套用到既有頁面，還是只套用在新 Story、既有頁面等
被觸碰到時再套用（建議後者，避免一張「重構全站 UI」的巨型 Story）。
